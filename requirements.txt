# ====================== Web 框架与 API ======================
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
click==8.4.2
exceptiongroup==1.3.1
fastapi==0.141.1
h11==0.16.0
httptools==0.8.0
idna==3.18
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
python-multipart==0.0.32   # /upload-pdf 等 Form/File 端点必需，缺失会导致 uvicorn 启动崩溃
PyYAML==6.0.3
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.52.1
uvloop==0.22.1
watchfiles==1.2.0   # 原 1.2.1 在 PyPI 不存在（yanked），修正为 1.2.0
websockets==16.1.1

# ====================== RAG / 向量检索（LangChain 1.x） ======================
langchain==1.3.15
langchain-core==1.5.5
langchain-community==0.4.2
langchain-classic==1.0.8
langchain-chroma==1.1.0
langchain-huggingface==1.2.2
langchain-ollama==1.1.0
langchain-text-splitters==1.1.2
chromadb==1.5.9

# ====================== 嵌入 / 生成模型 ======================
sentence-transformers==6.0.0
transformers==5.15.0
# 注意: torch 不在本文件锁定，由 Dockerfile 单独安装 CPU 版（--index-url whl/cpu），
#       避免从 PyPI 拉取体积巨大的 CUDA 版；本地环境已有 torch 2.13.0 可满足依赖。

# ====================== 数据处理 ======================
pandas==2.3.3
numpy==2.2.6
pyarrow==25.0.1
pyspark==3.5.9

# ====================== 新闻抓取（news_crawler / news_refresh） ======================
feedparser==6.0.14
beautifulsoup4==4.15.0
